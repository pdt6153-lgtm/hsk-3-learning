// Defaults for file:// or static hosting. The local server overrides this route at runtime.
window.HSK_TTS_ENDPOINT = window.HSK_TTS_ENDPOINT || '';
window.HSK_TTS_SUPPORTS_VOICES = window.HSK_TTS_SUPPORTS_VOICES === true;
